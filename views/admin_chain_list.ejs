<%- include('./include/_header') %>

<main class="admin-page">
  <div class="sg-container">
    <h1>Chain Ground 관리</h1>
    <p>가사 체인 목록을 확인하고 삭제할 수 있습니다.</p>

    <% if (!chains.length) { %>
      <p>등록된 체인이 없습니다.</p>
    <% } else { %>
      <table class="table table-striped table-dark">
        <thead>
          <tr>
            <th>가사</th>
            <th>제목 / 가수</th>
            <th>작성자</th>
            <th>작성일</th>
            <th>관리</th>
          </tr>
        </thead>
        <tbody>
          <% chains.forEach(c => { %>
            <tr>
              <td><%= c.lyric %></td>
              <td><%= c.title %> – <%= c.artist %></td>
              <td><%= c.user ? (c.user.username || c.user.name) : '알 수 없음' %></td>
              <td><%= c.createdAt.toLocaleString('ko-KR') %></td>
              <td>
                <form
                  action="/admin/chain/<%= c._id %>?_method=DELETE"
                  method="POST"
                  onsubmit="return confirm('정말 이 체인을 삭제하시겠습니까?');"
                >
                  <button type="submit" class="btn btn-sm btn-danger">삭제</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/admin" class="btn btn-secondary mt-3">관리자 홈으로</a>
  </div>
</main>

<%- include('./include/_footer') %>
